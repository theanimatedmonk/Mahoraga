# Design tokens — Breaking News Launcher

Extracted from Figma section **✅  ・ Final design v2.0 (June 16, 2026)** (`6622:22005`).

## Files

- `tokens.json` — full palette, typography, semantic light/dark tokens
- `light-mode.css` — CSS custom properties (light)
- `dark-mode.css` — CSS custom properties (dark, inferred)

## Note on dark mode

This section contains **light-mode screens only**. Dark tokens in `tokens.json` are suggested handoff values, not pulled from dark frames in Figma.
